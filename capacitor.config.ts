import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thecodez.baneverywhere',
  appName: 'baneverywhere',
  webDir: 'dist',
  bundledWebRuntime: true
};

export default config;
